const core = require('../lib/core');
const { CLIError } = require('../lib/utils');

const checkCmd = async (csvFilename, directory) => {
    throw CLIError.TODO('not implemented (yet)');
}

module.exports = checkCmd;